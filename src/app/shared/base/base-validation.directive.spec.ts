import { BaseValidationDirective } from './base-validation.directive';

describe('BaseValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseValidationDirective();
    expect(directive).toBeTruthy();
  });
});
