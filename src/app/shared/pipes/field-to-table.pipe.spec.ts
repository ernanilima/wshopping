import { FieldToTablePipe } from './field-to-table.pipe';

describe('FieldToTablePipe', () => {
  it('create an instance', () => {
    const pipe = new FieldToTablePipe();
    expect(pipe).toBeTruthy();
  });
});
